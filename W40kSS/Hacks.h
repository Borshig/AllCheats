#pragma once
#include <windows.h>
//#include <D3dx9core.h>
#include <d3d9.h>
#include <string>

/*
class Hacks
{
public:
	void DrawMenu(IDirect3DDevice9 * d3dDevice);
	void DrawBorderBox(int x, int y, int w, int h, int thickness, D3DCOLOR Color, IDirect3DDevice9 *pDevice);
	void CreateFront(IDirect3DDevice9* d3dDevice, std::string choiseFront);
private:
	hack

};
*/